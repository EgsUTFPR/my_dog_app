name: my_dog_app
description: "Aplicativo Flutter para gerenciamento de passeios de cães."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ^3.9.0

dependencies:
  flutter:
    sdk: flutter

  # 🔥 Firebase (versões compatíveis com Flutter 3.35)
  firebase_core: ^3.5.0
  firebase_auth: ^5.3.2
  cloud_firestore: ^5.4.4
  firebase_database: ^11.0.4

  # 📦 Gerenciamento de estado
  provider: ^6.1.2

  # 📍 Navegação moderna
  go_router: ^14.3.0

  # 🎨 Ícones e utilitários
  font_awesome_flutter: ^10.12.0
  path_provider: ^2.1.3
  intl: any
  cupertino_icons: ^1.0.8
  google_maps_flutter: ^2.14.0
  geocoding: ^4.0.0
  url_launcher: ^6.3.2
  geolocator: ^14.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - images/
